services:
  - type: web
    name: clipforge
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PORT
        value: 5000
    disk:
      name: clips-storage
      mountPath: /app/static
      sizeGB: 10
